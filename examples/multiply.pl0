
var
  x, y, res;

procedure multiply;
var a, b, z;
begin
  a := x;
  b := y;
  z := 0;
  while b > 0 do begin
    if odd b then z := z + a;
    a := 2 * a;
    b := b / 2
  end;
  res := z;
  ! res;
end;

begin
  x := 6;
  y := 5;
  call multiply;
end.
